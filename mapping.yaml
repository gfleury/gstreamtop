mappings:
- name: passwd
  tables:
  - CREATE TABLE user(gid INTEGER, shell VARCHAR) FIELDS IDENTIFIED BY '(?P<gid>[0-9]+):.*:(?P<shell>.[^:]*)$'
    LINES TERMINATED BY '\n';
  queries:
  - name: sumgig
    query: SELECT shell, COUNT(*), SUM(gid) FROM user GROUP BY shell;